language: node_js
node_js:
  - "7"

cache: npm

branches:
  except:
    - legacy
    - experimental

install:
  - npm install

script:
  - npm run process
  - npm run test
  - npm run build

deploy:
  local-dir: ./dist
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_API_KEY  
  keep-history: true
  on:
    branch: rss-mentor-dashboard